default-flavor: bullseye

template:
  build:
    builddeps: &builddeps
      nodejs:
      procps:
    post-cache:

bullseye:
  test:
    builddeps:
      <<: *builddeps
    post-cache:
      - yarn install
      - yarn pretty
      - yarn test
      - yarn database:create-mock && yarn test:ui
