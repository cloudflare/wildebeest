default-flavor: bullseye

template:
  build:
    builddeps: &builddeps
      nodejs:
    post-cache:

bullseye:
  test:
    builddeps:
      <<: *builddeps
    post-cache:
      - yarn install
      - npm run pretty
      - npm run test
