default-flavor: bullseye

template:
  build:
    builddeps: &builddeps
      ? nodejs
    post-cache:

bullseye:
  test:
    builddeps:
      <<: *builddeps
    post-cache:
      - npm install
      - npm run test
