// All other requests are handled by the UI, which is a server-side rendered app.
// The `onRequest` handler is created in the `ui/server` directory at build time.
// export { onRequest } from '../ui/server/entry.cloudflare-pages'
